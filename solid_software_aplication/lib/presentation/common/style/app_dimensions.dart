import 'package:get/get.dart';

class AppDimensions {
  static final double iconSize = Get.width * 0.05;
  static final double borderRadius = Get.width * 0.5;
  static final double fontSize = Get.height * 0.05 / Get.textScaleFactor;
  static final double allPadding = Get.width * 0.05;
}
